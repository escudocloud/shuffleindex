I dati sono memorizzati nel file secondo lo schema seguente:

1a colonna - numero di elementi in cache
2a colonna - numero di cover searches
3a colonna - tempo impiegato con l'algoritmo pir
4a colonna - tempo impiegato con l'algoritmo di ricerca naive

Calcolati con 100 ricerche per ogni combinazione cache/cover e profilo 0.5
I 2 file fanno riferimento allo stesso identico test ripetuto 2 volte.